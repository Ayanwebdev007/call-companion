name: call_companion_mobile
description: Mobile companion app for Call Companion CRM
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.2
  permission_handler: ^11.3.0
  call_log: ^6.0.1
  mobile_scanner: ^5.2.3
  socket_io_client: ^3.1.3
  url_launcher: ^6.3.0
  flutter_local_notifications: ^18.0.0
  phone_state: ^1.1.0
  overlay_support: ^2.1.0
  # workmanager: ^0.5.2 # For background tasks (commented out for simplicity, start with foreground sync)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0


flutter:
  uses-material-design: true
